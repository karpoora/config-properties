server:
  port: 8081

eureka:
  client:
    healthcheck:
      enabled: true

management:
  security:
    enabled: false

security:
  basic:
    enabled: false
  ignored:
    - console/**
    - health
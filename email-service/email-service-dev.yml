server:
  port: 8081

spring:
  boot:
    admin:
      discovery:
        services: server-admin

management:
  security:
    enabled: false

security:
  basic:
    enabled: false
  ignored:
    - console/**
    - health
